# Buggy Mobile App

The styling of this app is under developement, but you the minimal UI is still working.

## Run the project on your local

1. Install dependencies

   ```bash
   npm install
   ```

2. Start the app

   ```bash
   npx expo start
   ```
